<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.develop.mvp</groupId>
    <artifactId>develop-dependencies</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>
    <description>基础 bom 文件，管理整个项目的依赖版本</description>
    <url>https://github.com/YunaiV/ruoyi-vue-pro</url>

    <!--
        BOM 治理边界：本模块只负责“版本与依赖管理”，不承载业务代码、不声明运行时启动入口。
        所有业务模块、框架 starter、server/gateway 应通过父级依赖管理继承这里的版本，避免在子模块重复硬编码版本。
        调整原则：先确认 Spring Boot、Spring Cloud、Spring Cloud Alibaba 三条主版本兼容矩阵，再同步校验数据库、观测、测试和安全相关依赖。
    -->
    <properties>
        <!-- 当前仓库内部 Maven 模块统一版本；flatten 插件会在发布/打包时把 ${revision} 展平为确定版本。 -->
        <revision>2026.04-SNAPSHOT</revision>
        <flatten-maven-plugin.version>1.7.2</flatten-maven-plugin.version>
        <!-- 平台基线：三者共同决定 Spring 生态依赖上限，升级时必须作为一个兼容组合验证。 -->
        <spring.boot.version>3.5.14</spring.boot.version>
        <spring.cloud.version>2025.0.1</spring.cloud.version>
        <spring.cloud.alibaba.version>2025.0.0.0</spring.cloud.alibaba.version>
        <!-- Web/API 文档：治理 MVC/OpenAPI/Knife4j 版本，供 web starter、server 与 gateway 统一生成接口文档。 -->
        <springdoc.version>2.8.17</springdoc.version>
        <knife4j.version>4.5.0</knife4j.version>
        <!-- 数据访问与缓存：治理连接池、ORM、多数据源、数据翻译、Redis/Redisson 以及国产数据库驱动。 -->
        <druid.version>1.2.28</druid.version>
        <mybatis.version>3.5.19</mybatis.version>
        <mybatis-plus.version>3.5.16</mybatis-plus.version>
        <mybatis-plus-join.version>1.5.7</mybatis-plus-join.version>
        <dynamic-datasource.version>4.5.0</dynamic-datasource.version>
        <easy-trans.version>3.0.6</easy-trans.version>
        <redisson.version>4.3.1</redisson.version>
        <dm8.jdbc.version>8.1.3.140</dm8.jdbc.version>
        <kingbase.jdbc.version>9.0.1.jre7</kingbase.jdbc.version>
        <opengauss.jdbc.version>7.0.0-RC3-og</opengauss.jdbc.version>
        <taos.version>3.8.3</taos.version>
        <!-- 消息队列：治理 MQ starter 外部实现版本；内部 develop-spring-boot-starter-mq 版本跟随 revision。 -->
        <rocketmq-spring.version>2.3.5</rocketmq-spring.version>
        <!-- RPC：远程调用 starter 当前只治理内部模块版本，外部 Feign/Spring Cloud 版本由 Spring Cloud BOM 接管。 -->
        <!-- Config/Registry：Nacos 等注册配置中心依赖由 Spring Cloud Alibaba BOM 接管，避免重复指定版本。 -->
        <!-- Job：治理 XXL-Job 核心依赖版本，内部定时任务 starter 版本跟随 revision。 -->
        <xxl-job.version>2.4.0</xxl-job.version>
        <!-- 服务保障：治理限流熔断、分布式锁等可靠性组件版本，避免与 Redis/Redisson 版本漂移。 -->
        <lock4j.version>2.2.7</lock4j.version>
        <!-- 可观测性：治理链路追踪、日志埋点和 Spring Boot Admin 版本，升级时需验证 actuator/日志栈兼容性。 -->
        <skywalking.version>9.6.0</skywalking.version>
        <spring-boot-admin.version>3.5.8</spring-boot-admin.version>
        <opentracing.version>0.33.0</opentracing.version>
        <!-- 测试体系：治理单元测试、架构约束测试、Mock 与测试数据生成工具版本。 -->
        <podam.version>8.0.2.RELEASE</podam.version>
        <jedis-mock.version>1.1.12</jedis-mock.version>
        <mockito-inline.version>5.2.0</mockito-inline.version>
        <archunit.version>1.4.2</archunit.version>
        <!-- BPM/工作流：治理 Flowable 引擎与 actuator 集成版本，升级时需验证流程定义、任务执行和监控端点兼容性。 -->
        <flowable.version>7.2.0</flowable.version>
        <!-- 通用工具与协议栈：治理验证码、HTML 解析、编译期工具、对象映射、Excel、模板、安全修复、网络协议和 IoT 通信依赖。 -->
        <anji-plus-captcha.version>1.4.0</anji-plus-captcha.version>
        <jsoup.version>1.22.2</jsoup.version>
        <lombok.version>1.18.46</lombok.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <hutool-5.version>5.8.44</hutool-5.version>
        <hutool-6.version>6.0.0-M22</hutool-6.version>
        <fastexcel.version>1.3.0</fastexcel.version>
        <velocity.version>2.4.1</velocity.version>
        <fastjson.version>1.2.83</fastjson.version>
        <guava.version>33.6.0-jre</guava.version>
        <transmittable-thread-local.version>2.14.5</transmittable-thread-local.version>
        <commons-net.version>3.13.0</commons-net.version>
        <commons-lang3.version>3.20.0</commons-lang3.version>
        <jsch.version>2.28.2</jsch.version>
        <tika-core.version>3.3.0</tika-core.version>
        <ip2region.version>2.7.0</ip2region.version>
        <bizlog-sdk.version>3.0.6</bizlog-sdk.version>
        <reflections.version>0.10.2</reflections.version>
        <netty.version>4.2.12.Final</netty.version>
        <mqtt.version>1.2.5</mqtt.version>
        <vertx.version>4.5.26</vertx.version>
        <okhttp.version>4.12.0</okhttp.version>
        <californium.version>3.14.0</californium.version>
        <j2mod.version>3.3.0</j2mod.version>
        <!-- 三方云服务与支付登录：治理对象存储、OAuth 登录、积木报表、微信生态与支付宝 SDK 版本。 -->
        <awssdk.version>2.44.0</awssdk.version>
        <justauth.version>1.16.7</justauth.version>
        <justauth-starter.version>1.4.0</justauth-starter.version>
        <jimureport.version>2.3.2</jimureport.version>
        <jimubi.version>2.3.2</jimubi.version>
        <weixin-java.version>4.8.2-20260501.180637</weixin-java.version>
        <alipay-sdk-java.version>4.40.771.ALL</alipay-sdk-java.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
                上游 BOM 导入顺序：先导入底层 Netty，再导入 Spring Boot、Spring Cloud、Spring Cloud Alibaba。
                子模块不应绕过这些 BOM 单独声明 Spring/Cloud/Alibaba 生态依赖版本；确需覆盖时必须在这里集中治理并说明原因。
            -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring.cloud.alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
                业务公共能力：治理多租户、数据权限、IP 归属地、业务日志等跨模块能力。
                内部 starter 使用 ${revision}，保证 develop-framework 与业务模块在同一仓库版本线上演进。
            -->
            <dependency>
                <groupId>io.github.mouzt</groupId>
                <artifactId>bizlog-sdk</artifactId>
                <version>${bizlog-sdk.version}</version>
                <exclusions>
                    <exclusion> <!-- 排除 bizlog 自带 Spring Boot starter，统一使用本项目 Spring Boot BOM 管理的版本。 -->
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-starter</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-biz-tenant</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-biz-data-permission</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-biz-ip</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- Spring 核心：治理配置元数据生成器与环境 starter；业务模块只消费 starter，不在本地重复声明版本。 -->
            <dependency>
                <!-- 用于生成自定义的 Spring @ConfigurationProperties 配置类的说明文件 -->
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-configuration-processor</artifactId>
                <version>${spring.boot.version}</version>
            </dependency>

            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-env</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- Web/API 文档：治理 MVC/OpenAPI/Knife4j 版本，供 web starter、server 与 gateway 统一生成接口文档。 -->
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-web</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-security</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-websocket</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
                <exclusions>
                    <exclusion>
                        <!-- Springdoc UI 版本由下方单独锁定，避免 Knife4j 传递版本与平台 Spring Boot 版本不匹配。 -->
                        <groupId>org.springdoc</groupId>
                        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.xiaoymin</groupId> <!-- 接口文档 UI：knife4j【网关专属】 -->
                <artifactId>knife4j-gateway-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>

            <!-- 数据访问与缓存：治理连接池、ORM、多数据源、数据翻译、Redis/Redisson 以及国产数据库驱动。 -->
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-mybatis</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>
            <dependency>
                <!-- 注意：必须声明，避免 flowable 和 mybatis-plus 引入的 mybatis 版本不一致！！！ -->
                <groupId>org.mybatis</groupId>
                <artifactId>mybatis</artifactId>
                <version>${mybatis.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-generator</artifactId> <!-- 代码生成器，使用它解析表结构 -->
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>dynamic-datasource-spring-boot3-starter</artifactId> <!-- 多数据源 -->
                <version>${dynamic-datasource.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.yulichang</groupId>
                <artifactId>mybatis-plus-join-boot-starter</artifactId> <!-- MyBatis 联表查询 -->
                <version>${mybatis-plus-join.version}</version>
            </dependency>

            <dependency>
                <groupId>com.fhs-opensource</groupId> <!-- VO 数据翻译 -->
                <artifactId>easy-trans-spring-boot-starter</artifactId>
                <version>${easy-trans.version}</version>
                <exclusions>
                    <exclusion>
                        <!-- Spring Framework 版本由 Spring Boot BOM 统一治理，避免 easy-trans 传递版本覆盖。 -->
                        <groupId>org.springframework</groupId>
                        <artifactId>spring-context</artifactId>
                    </exclusion>
                    <exclusion>
                        <!-- Spring Cloud 公共包版本由 Spring Cloud BOM 统一治理，避免数据翻译组件带入旧版本。 -->
                        <groupId>org.springframework.cloud</groupId>
                        <artifactId>spring-cloud-commons</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>com.fhs-opensource</groupId>
                <artifactId>easy-trans-mybatis-plus-extend</artifactId>
                <version>${easy-trans.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fhs-opensource</groupId>
                <artifactId>easy-trans-anno</artifactId>
                <version>${easy-trans.version}</version>
            </dependency>

            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-redis</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>

            <dependency>
                <groupId>com.dameng</groupId>
                <artifactId>DmJdbcDriver18</artifactId>
                <version>${dm8.jdbc.version}</version>
            </dependency>

            <dependency>
                <groupId>org.opengauss</groupId>
                <artifactId>opengauss-jdbc</artifactId>
                <version>${opengauss.jdbc.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.com.kingbase</groupId>
                <artifactId>kingbase8</artifactId>
                <version>${kingbase.jdbc.version}</version>
            </dependency>

            <dependency>
                <groupId>com.taosdata.jdbc</groupId>
                <artifactId>taos-jdbcdriver</artifactId>
                <version>${taos.version}</version>
            </dependency>

            <!-- RPC：治理远程调用 starter；Feign、负载均衡等外部版本由 Spring Cloud BOM 统一接管。 -->
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-rpc</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- Registry：注册中心依赖当前由 Spring Cloud Alibaba BOM 管理，本 BOM 暂不额外覆盖具体坐标。 -->

            <!-- Config：配置中心依赖当前由 Spring Cloud Alibaba BOM 管理，本 BOM 暂不额外覆盖具体坐标。 -->

            <!-- Job：治理 XXL-Job 执行器核心包与项目定时任务 starter，业务模块通过 starter 接入任务调度。 -->
            <dependency>
                <groupId>com.xuxueli</groupId>
                <artifactId>xxl-job-core</artifactId>
                <version>${xxl-job.version}</version>
            </dependency>
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-job</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- 消息队列：治理项目 MQ starter 与 RocketMQ Spring Boot starter，其他 MQ 实现如由 starter 间接引入则在对应 starter 内隔离。 -->
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-mq</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.rocketmq</groupId>
                <artifactId>rocketmq-spring-boot-starter</artifactId>
                <version>${rocketmq-spring.version}</version>
            </dependency>

            <!-- 服务保障：治理限流熔断、分布式锁等可靠性组件版本，避免与 Redis/Redisson 版本漂移。 -->
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-protection</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>lock4j-redisson-spring-boot-starter</artifactId>
                <version>${lock4j.version}</version>
                <exclusions>
                    <exclusion>
                        <!-- Redisson 版本由上方 redisson-spring-boot-starter 统一锁定，避免 lock4j 传递版本覆盖。 -->
                        <artifactId>redisson-spring-boot-starter</artifactId>
                        <groupId>org.redisson</groupId>
                    </exclusion>
                </exclusions>
            </dependency>

            <!-- 可观测性：治理链路追踪、日志埋点和 Spring Boot Admin 版本，升级时需验证 actuator/日志栈兼容性。 -->
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-monitor</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.skywalking</groupId>
                <artifactId>apm-toolkit-trace</artifactId>
                <version>${skywalking.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.skywalking</groupId>
                <artifactId>apm-toolkit-logback-1.x</artifactId>
                <version>${skywalking.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.skywalking</groupId>
                <artifactId>apm-toolkit-opentracing</artifactId>
                <version>${skywalking.version}</version>
                <!--                <exclusions>-->
                <!--                    <exclusion>-->
                <!--                        <artifactId>opentracing-api</artifactId>-->
                <!--                        <groupId>io.opentracing</groupId>-->
                <!--                    </exclusion>-->
                <!--                    <exclusion>-->
                <!--                        <artifactId>opentracing-util</artifactId>-->
                <!--                        <groupId>io.opentracing</groupId>-->
                <!--                    </exclusion>-->
                <!--                </exclusions>-->
            </dependency>
            <dependency>
                <groupId>io.opentracing</groupId>
                <artifactId>opentracing-api</artifactId>
                <version>${opentracing.version}</version>
            </dependency>
            <dependency>
                <groupId>io.opentracing</groupId>
                <artifactId>opentracing-util</artifactId>
                <version>${opentracing.version}</version>
            </dependency>
            <dependency>
                <groupId>io.opentracing</groupId>
                <artifactId>opentracing-noop</artifactId>
                <version>${opentracing.version}</version>
            </dependency>

            <dependency>
                <groupId>de.codecentric</groupId>
                <artifactId>spring-boot-admin-starter-server</artifactId> <!-- 实现 Spring Boot Admin Server 服务端 -->
                <version>${spring-boot-admin.version}</version>
            </dependency>
            <dependency>
                <groupId>de.codecentric</groupId>
                <artifactId>spring-boot-admin-starter-client</artifactId> <!-- 实现 Spring Boot Admin Client 客户端上报 -->
                <version>${spring-boot-admin.version}</version>
            </dependency>

            <!-- 测试体系：治理单元测试、架构约束测试、Mock 与测试数据生成工具版本。 -->
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-test</artifactId>
                <version>${revision}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-inline</artifactId>
                <version>${mockito-inline.version}</version> <!-- 支持 Mockito 的 final 类与 static 方法的 mock -->
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-test</artifactId>
                <version>${spring.boot.version}</version>
                <exclusions>
                    <exclusion>
                        <!-- ASM 由 JUnit/ArchUnit 等测试链路按兼容版本引入，避免 starter-test 传递版本造成冲突。 -->
                        <artifactId>asm</artifactId>
                        <groupId>org.ow2.asm</groupId>
                    </exclusion>
                    <exclusion>
                        <!-- 使用 mockito-inline 统一支持 final/static mock，排除默认 mockito-core 防止双版本并存。 -->
                        <groupId>org.mockito</groupId>
                        <artifactId>mockito-core</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
            </dependency>

            <dependency>
                <groupId>com.github.fppt</groupId> <!-- 单元测试，我们采用内嵌的 Redis 数据库 -->
                <artifactId>jedis-mock</artifactId>
                <version>${jedis-mock.version}</version>
            </dependency>

            <dependency>
                <groupId>uk.co.jemos.podam</groupId> <!-- 单元测试，随机生成 POJO 类 -->
                <artifactId>podam</artifactId>
                <version>${podam.version}</version>
            </dependency>

            <!-- BPM/工作流：治理 Flowable 流程引擎与 actuator 集成，避免流程模块和监控模块各自漂移版本。 -->
            <dependency>
                <groupId>org.flowable</groupId>
                <artifactId>flowable-spring-boot-starter-process</artifactId>
                <version>${flowable.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flowable</groupId>
                <artifactId>flowable-spring-boot-starter-actuator</artifactId>
                <version>${flowable.version}</version>
            </dependency>
            <!-- BPM/工作流结束：新增 Flowable 相关依赖时应放在此段并共用 flowable.version。 -->

            <!--
                通用工具与协议栈：治理 develop-common、Excel starter、编译期注解处理、集合/模板/文件识别、安全修复、网络客户端与 IoT 协议依赖。
                业务模块优先依赖 develop-common 或对应 starter；只有明确需要第三方能力时才直接声明下方依赖。
            -->
            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-common</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.develop.mvp</groupId>
                <artifactId>develop-spring-boot-starter-excel</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId> <!-- use mapstruct-jdk8 for Java 8 or higher -->
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-jdk8</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool-5.version}</version>
            </dependency>
            <dependency>
                <groupId>org.dromara.hutool</groupId>
                <artifactId>hutool-extra</artifactId>
                <version>${hutool-6.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.idev.excel</groupId>
                <artifactId>fastexcel</artifactId>
                <version>${fastexcel.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.tika</groupId>
                <artifactId>tika-core</artifactId> <!-- 文件类型的识别 -->
                <version>${tika-core.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>

            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>fastjson</artifactId>
                <version>${fastjson.version}</version>
            </dependency>

            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>

            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>transmittable-thread-local</artifactId> <!-- 解决 ThreadLocal 父子线程的传值问题 -->
                <version>${transmittable-thread-local.version}</version>
            </dependency>

            <dependency>
                <groupId>commons-net</groupId>
                <artifactId>commons-net</artifactId> <!-- 解决 ftp 连接 -->
                <version>${commons-net.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.mwiede</groupId>
                <artifactId>jsch</artifactId> <!-- 解决 sftp 连接 -->
                <version>${jsch.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version> <!-- 解决 CVE-2025-48924 漏洞 -->
            </dependency>

            <dependency>
                <groupId>com.anji-plus</groupId>
                <artifactId>captcha-spring-boot-starter</artifactId> <!-- 验证码，一般用于登录使用 -->
                <version>${anji-plus-captcha.version}</version>
            </dependency>

            <dependency>
                <groupId>org.lionsoul</groupId>
                <artifactId>ip2region</artifactId>
                <version>${ip2region.version}</version>
            </dependency>

            <dependency>
                <groupId>org.jsoup</groupId>
                <artifactId>jsoup</artifactId>
                <version>${jsoup.version}</version>
            </dependency>

            <dependency>
                <groupId>org.reflections</groupId>
                <artifactId>reflections</artifactId>
                <version>${reflections.version}</version>
            </dependency>

            <!-- Vert.x：IoT/异步网络场景使用的响应式协议栈，core/web/mqtt 必须保持同一版本。 -->
            <dependency>
                <groupId>io.vertx</groupId>
                <artifactId>vertx-core</artifactId>
                <version>${vertx.version}</version>
            </dependency>
            <dependency>
                <groupId>io.vertx</groupId>
                <artifactId>vertx-web</artifactId>
                <version>${vertx.version}</version>
            </dependency>
            <dependency>
                <groupId>io.vertx</groupId>
                <artifactId>vertx-mqtt</artifactId>
                <version>${vertx.version}</version>
            </dependency>

            <!-- MQTT：Paho 客户端用于轻量消息协议接入，版本需与 IoT 模块连接管理能力一起验证。 -->
            <dependency>
                <groupId>org.eclipse.paho</groupId>
                <artifactId>org.eclipse.paho.client.mqttv3</artifactId>
                <version>${mqtt.version}</version>
            </dependency>

            <!-- OkHttp：统一 HTTP 客户端与 MockWebServer 测试版本，避免测试依赖和运行时客户端版本不一致；MockWebServer 只用于 test scope。 -->
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>okhttp</artifactId>
                <version>${okhttp.version}</version>
            </dependency>
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>mockwebserver</artifactId>
                <version>${okhttp.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- CoAP：Eclipse Californium 用于受限设备协议通信，升级时需验证 IoT 设备接入与消息编解码。 -->
            <dependency>
                <groupId>org.eclipse.californium</groupId>
                <artifactId>californium-core</artifactId>
                <version>${californium.version}</version>
            </dependency>

            <!-- Modbus：j2mod 用于工业设备协议通信，升级时需验证串口/TCP 采集链路。 -->
            <dependency>
                <groupId>com.ghgande</groupId>
                <artifactId>j2mod</artifactId>
                <version>${j2mod.version}</version>
            </dependency>


            <!--
                三方云服务与支付登录：治理 S3、OAuth、支付宝、微信支付/公众号/小程序、积木报表 BI 依赖。
                这些 SDK 经常携带旧版加密、JSON、Hutool 或 SQL 解析器依赖；如发生冲突，应在这里集中排除并说明原因。
            -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>s3</artifactId>
                <version>${awssdk.version}</version>
            </dependency>

            <dependency>
                <groupId>me.zhyd.oauth</groupId>
                <artifactId>JustAuth</artifactId> <!-- 社交登陆（例如说，个人微信、企业微信等等） -->
                <version>${justauth.version}</version>
            </dependency>
            <dependency>
                <groupId>com.xkcoding.justauth</groupId>
                <artifactId>justauth-spring-boot-starter</artifactId>
                <version>${justauth-starter.version}</version>
                <exclusions>
                    <!-- 移除 starter 传递的 hutool-core，统一使用本 BOM 中声明的 hutool-all/hutool-extra 组合。 -->
                    <exclusion>
                        <groupId>cn.hutool</groupId>
                        <artifactId>hutool-core</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <dependency>
                <groupId>com.alipay.sdk</groupId>
                <artifactId>alipay-sdk-java</artifactId>
                <version>${alipay-sdk-java.version}</version>
                <exclusions>
                    <exclusion>
                        <!-- 排除旧 JDK 线的 BouncyCastle 传递依赖，避免和 Java 17 兼容的安全加密依赖策略冲突。 -->
                        <groupId>org.bouncycastle</groupId>
                        <artifactId>bcprov-jdk15on</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <dependency>
                <groupId>com.github.binarywang</groupId>
                <artifactId>weixin-java-pay</artifactId>
                <version>${weixin-java.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.binarywang</groupId>
                <artifactId>wx-java-mp-spring-boot-starter</artifactId>
                <version>${weixin-java.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.binarywang</groupId>
                <artifactId>wx-java-miniapp-spring-boot-starter</artifactId>
                <version>${weixin-java.version}</version>
            </dependency>

            <!-- 积木报表/BI：报表服务与 BI starter 共用版本，需注意其传递 SQL 解析器与 Hutool 版本冲突。 -->
            <dependency>
                <groupId>org.jeecgframework.jimureport</groupId>
                <artifactId>jimureport-spring-boot3-starter</artifactId>
                <version>${jimureport.version}</version>
            </dependency>
            <dependency>
                <groupId>org.jeecgframework.jimureport</groupId>
                <artifactId>jimubi-spring-boot3-starter</artifactId>
                <version>${jimubi.version}</version>
                <exclusions>
                    <exclusion>
                        <!-- SQL 解析器版本由 MyBatis Plus/JSqlParser 链路统一治理，避免 BI starter 传递版本覆盖。 -->
                        <groupId>com.github.jsqlparser</groupId>
                        <artifactId>jsqlparser</artifactId>
                    </exclusion>
                    <exclusion>
                        <!-- Hutool 版本由本 BOM 的 hutool-all/hutool-extra 组合统一治理。 -->
                        <groupId>cn.hutool</groupId>
                        <artifactId>hutool-core</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <!-- 统一 revision 版本 -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>${flatten-maven-plugin.version}</version>
                <configuration>
                    <flattenMode>bom</flattenMode>
                    <updatePomFile>true</updatePomFile>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                    </execution>
                    <execution>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
